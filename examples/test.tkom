// some high order function examples
void main {
    for (0 => mut int i; i < 20) {
        (i) -> some_func;
    } -> increment @ print_current_val;
}

[void] print_current_val :: [void::mut int] func, mut int a {
    (a) -> func;
    (a + "\n") -> stdout;
}
